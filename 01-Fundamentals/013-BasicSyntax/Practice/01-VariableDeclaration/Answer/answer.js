// Declare a `const` variable named `favoriteColor` and assign it your favorite color as a string.
const favoriteColor = "Red";
// Declare a `let` variable named `age` and assign it your age as a number.
let age = 23;
// Declare a `let` variable named `isStudent` and assign it a boolean value based on whether you are currently a student.
let isStudent = true;

// Print the values of all three variables to the console.
console.log(favoriteColor);
console.log(age);
console.log(isStudent);
