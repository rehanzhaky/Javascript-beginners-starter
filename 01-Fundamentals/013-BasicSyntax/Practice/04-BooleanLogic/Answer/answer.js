// Create two variables: `hasLicense` and `isInsured`, and assign them boolean values based on whether you have a driver's license and car insurance.
let hasLicense = true;
let isInsured = false;

// Create a variable `canDrive` that is true only if both `hasLicense` and `isInsured` are true.
let canDrive = hasLicense && isInsured;

// Print the value of `canDrive` to the console.
console.log(canDrive);